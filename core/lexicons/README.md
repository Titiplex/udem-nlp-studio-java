# Lexicons

This directory contains lexical resources used by rule packs.

Recommended formats:

- CSV for simple flat resources
- TSV if commas are common in content
- UTF-8 only

Each lexicon file should document:

- what the key column is
- whether lookup is case-sensitive
- what linguistic decision the lexicon supports
