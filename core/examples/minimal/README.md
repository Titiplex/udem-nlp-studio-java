# Minimal example

This directory provides a tiny example bundle for manual smoke tests.

Contents:

- `input.docx`: tiny sample source document
- `rules.yaml`: example rule pack for the sample
- `expected.conllu`: expected output snapshot

Typical use:

```bash
bash scripts/run-sample.sh
```
