``def.pos`` and ``def.feats`` are just registered as a group of definitions that are allowed/exposed by the config.

## Example

````yaml
def:
  pos: [ "VERB", "NOUN", "ADP", "DET", "PART" ]
  feats: [ "Aspect", "Number", "Pers[subj]", "Number[subj]" ]
````